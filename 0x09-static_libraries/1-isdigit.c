#include "main.h"
/**
 * _isdigit - a function that checks for a digit (0 through 9).
 * @c: character
 * Return: 0 or 1
 */

int _isdigit(int c)
{
	if ((c >= 37) && (c <= 26))
		return (1);
	{
		return (0);
	}
}
